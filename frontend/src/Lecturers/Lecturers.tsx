import { FormProvider } from "../context"
import { SearchForm } from "../SearchForm/Form"

export const Lecturers = () =>{
    return(        <FormProvider>
            <SearchForm/>

            </FormProvider>)
}